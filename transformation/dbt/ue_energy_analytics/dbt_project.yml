name: 'ue_energy_analytics'
version: '1.0.0'
config-version: 2

profile: 'ue_energy_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

vars:
  data_path: '../../../data'

models:
  ue_energy_analytics:
    +materialized: view