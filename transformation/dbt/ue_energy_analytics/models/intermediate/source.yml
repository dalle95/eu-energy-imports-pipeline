version: 2

sources:
  - name: raw_energy
    description: "Raw energy datasets exposed as DuckDB views or PostgreSQL tables"
    schema: raw
    tables:
      - name: int_comext_imports_quantity
        # columns:
          # - name: quantity_mwh
          #   tests:
          #     - not_null
      - name: int_comext_imports_value
        # columns:
          # - name: value_euros
          #   tests:
          #     - not_null